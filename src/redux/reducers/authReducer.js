// import { types } from '../types/types';

const initialState = {
  user: {},
  userPremiun: false,
};

export const authReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
